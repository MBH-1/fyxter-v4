# 1) Allow Netlify Functions (Stripe, emails, backend logic)
 /.netlify/functions/*    /.netlify/functions/:splat    200

# 2) Dynamic repair pages (React routes + SEO pages)
 /repair/*                /index.html                   200

# 3) All other React routes (homepage, device pages, quartier pages)
 /*                       /index.html                   200
